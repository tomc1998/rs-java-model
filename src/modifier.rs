
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Modifier {
    Transient,
    Volatile,
    Public,
    Protected,
    Private,
    Abstract,
    Static,
    Final,
    Synchronized,
    Native,
    Strictfp,
}

